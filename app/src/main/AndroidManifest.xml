<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nerme.alabdallah">

    <application
        android:allowBackup="true"
        android:icon="@drawable/racing"
        android:label="@string/app_name"
        android:roundIcon="@drawable/abd"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HOME">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BRANDS" />
        <activity android:name=".COMPANIES" />
        <activity android:name=".producttap" />
        <activity android:name=".Models" />
        <activity android:name=".AddClient" />
        <activity android:name=".AddStocks" />
        <activity android:name=".ClientList" />
        <activity android:name=".StockList"></activity>
    </application>

</manifest>